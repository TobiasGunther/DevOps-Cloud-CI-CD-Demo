{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Demo": {
    "EnvironmentName": "Local",
    "Message": "Running from a developer machine, not from a pipeline."
  }
}
